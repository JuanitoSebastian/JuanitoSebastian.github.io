---
export interface Props {
    tag: string;
}

const { tag } = Astro.props
---
<div class="flex flex-row gap-4 text-sm border-b-2 border-gray-200">
    <p class="font-medium lowercase text-gray-600 font-mono">#{tag}</p>
</div>