---
import { Image } from 'astro:assets';
import bgSrc from '../../images/hero/juan_bg.webp';
import juanSrc from '../../images/hero/juan.webp';
import BlurryCircle from './BlurryCircle.astro';
import TitleTags from './TitleTags.astro';
---

<style lang="scss">
  @import '../../styles/variables';

  .hero {
    width: 100%;
    height: clamp(500px, 79.75vh, 638px);
    position: relative;
    margin-bottom: calc(var(--spacing-xxlg) * -1);
    display: flex;
    justify-content: center;
    overflow: hidden;
  }

  .hero-content {
    position: relative;
    width: 100%;
    height: 100%;
    max-width: var(--main-width);
  }

  .hero-image {
    width: calc(100% + clamp(0px, calc((960px - 100vw) * 0.7), 300px));
    height: 100%;
    object-fit: cover;
    display: block;
    transform: translateX(clamp(-300px, calc((100vw - 960px) * 0.5), 0px));
  }

  .bg {
    position: relative;
    z-index: var(--z-index-hero-bg);
    filter: var(--img-filter);
  }

  .juan {
    position: absolute;
    top: 0;
    left: 0;
    z-index: var(--z-index-hero-juan);
    pointer-events: none;
  }

  @media (min-width: #{$breakpoint-lg}) {
    .hero {
      margin-top: var(--spacing-lg);
    }
  }
</style>

<section class="hero" id="hero">
  <div class="hero-content">
    <Image src={bgSrc} alt="" class="hero-image bg" />
    <Image src={juanSrc} alt="" class="hero-image juan" />
    <BlurryCircle
      size="medium"
      color="var(--blurry-circle-blue)"
      bottom="10px"
      left="-50px"
      opacity={1}
    />
    <BlurryCircle
      size="large"
      color="var(--blurry-circle-green)"
      bottom="50px"
      right="-100px"
      opacity={0.7}
    />
    <TitleTags />
  </div>
</section>
